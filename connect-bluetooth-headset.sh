# TODO: the user will run the script with the name of the headset as a flag (i.e. MY NICE HEADSET) and the script will connect the headset, if it exists, if not, the script will say that didn't found
# NOTE: bluetoothctl "scan | remove | connect" will all be used for this tiny script 
